# Phantom FTMO

This folder is reserved for FTMO-specific live files (for example `PhantomBridge_FTMO.mq5`).

If the bridge source was lost during a reset, re-add it here before syncing/compiling to MT5.
